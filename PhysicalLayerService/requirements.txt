# Mirrored from container-root requirements to avoid drift.
# FastAPI stack
fastapi
uvicorn

# Pydantic v2 and settings management
pydantic>=2,<3
pydantic-settings>=2.0.0

# .env loading support
python-dotenv
