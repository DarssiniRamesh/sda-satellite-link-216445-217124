"""
Package initializer for the PhysicalLayerService repository module.

Having this file allows imports like `from PhysicalLayerService.app.main import app`
when running from the repository root, enabling preview runners to use `uvicorn main:app`.
"""
