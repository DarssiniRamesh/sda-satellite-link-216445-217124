"""
Application package for PhysicalLayerService.

Having this file ensures `app` is recognized as a package, enabling imports such as:
  - from app.main import app      (when run from the PhysicalLayerService folder)
  - from PhysicalLayerService.app.main import app  (when run from repo root)
"""
