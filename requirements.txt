# Root-level requirements for the PhysicalLayerService container.
# This file is used by preview/CI installers that run pip install from the container root.
# It mirrors the essential runtime dependencies required by the service.

# FastAPI stack
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1

# Pydantic v2 and settings management
pydantic>=2.0,<3
pydantic-settings>=2.0.0,<3

# .env loading support
python-dotenv>=1.0,<2
