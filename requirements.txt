# Root-level requirements for the PhysicalLayerService container.
# This file is used by preview/CI installers that run `pip install -r requirements.txt` from the container root.
# Ensure these exactly match required runtime deps and correct package names.

# FastAPI stack
fastapi
uvicorn

# Pydantic v2 and settings management (correct package name uses hyphen)
pydantic>=2,<3
pydantic-settings>=2,<3

# .env loading support (used by BaseSettings env_file behavior and local runs)
python-dotenv
